// import { getReplies } from "@/server/data"
// import { useQuery } from "@tanstack/react-query"

// import { Comment } from "@/types/db_tables"

// export function useReplies(comment_id: Comment["comment_id"]) {
//   const queryKey = ["replies", comment_id]

//   return useQuery({
//     queryKey,
//     queryFn: () => getReplies(comment_id),
//   })
// }
